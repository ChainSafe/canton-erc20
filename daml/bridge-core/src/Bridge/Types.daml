module Bridge.Types where

-- Re-export common types for convenience
-- Bridge-specific types can be added here

import Common.Types

-- Re-export common types used in bridge operations
type ChainReference = ChainRef
type EvmAddr = EvmAddress
type Direction = BridgeDirection
