{
  ledger-api {
    address = "localhost"
    port = 6865
  }
  server {
    address = "127.0.0.1"
    port = 7575
  }
  allow-insecure-tokens = true        # keep only for local HTTP testing
  auth-services = [
    {
      type = unsafe-jwt-hmac-256
      secret = "dev-secret"
    }
  ]
}